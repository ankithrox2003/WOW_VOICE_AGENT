# TEMPLATE ONLY - do not put real values in this file.
#
# Copy this to a file named exactly ".env" (no ".example") and fill that in.
# Only ".env" is read by the app, and only ".env" is gitignored.
#
#   copy .env.example .env

# From https://elevenlabs.io -> profile icon -> "API Keys"
ELEVENLABS_API_KEY=

# Run `python list_voices.py` to see the voice IDs on your account.
ELEVENLABS_VOICE_ID=
